<?= $this->extend('layouts/admin_master') ?>
<?= $this->section('content') ?>
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h4 mb-1">API đối tác & tích hợp</h1><p class="text-muted mb-0">Cấp quyền đọc dữ liệu công khai cho CLB, truyền thông và ứng dụng đối tác.</p></div><span class="badge text-bg-success">P8</span></div>
<div class="alert alert-info">Key chỉ hiển thị một lần sau khi tạo. Hệ thống chỉ lưu SHA-256, không lưu secret gốc.</div>
<div class="card shadow-sm mb-4"><div class="card-header d-flex justify-content-between"><strong>Integration của tenant</strong><span class="text-muted small">Health check không gửi credential ra ngoài</span></div><div class="table-responsive"><table class="table table-sm align-middle mb-0"><thead><tr><th>Integration</th><th>Loại</th><th>Trạng thái</th><th>Kiểm tra gần nhất</th><th></th></tr></thead><tbody><?php foreach ($integrations as $integration): ?><tr><td><strong><?= esc($integration->name) ?></strong><br><code><?= esc($integration->code) ?></code></td><td><?= esc($integration->provider_type) ?></td><td><span class="badge text-bg-<?= $integration->status === 'active' ? 'success' : 'secondary' ?>"><?= esc($integration->status) ?></span></td><td><?= esc($integration->last_sync_at ?: 'Chưa kiểm tra') ?><?php if ($integration->last_error): ?><br><small class="text-danger"><?= esc($integration->last_error) ?></small><?php endif; ?></td><td><form method="post" action="<?= base_url('admin/integrations/health/' . $integration->id) ?>"><?= csrf_field() ?><button class="btn btn-sm btn-outline-primary">Kiểm tra</button></form></td></tr><?php endforeach; ?></tbody></table></div></div>
<div class="card shadow-sm mb-4"><div class="card-body"><form method="post" action="<?= base_url('admin/integrations/keys') ?>" class="row g-2 align-items-end"><?= csrf_field() ?><div class="col-md-3"><label class="form-label">Tên ứng dụng</label><input name="name" class="form-control" placeholder="Ứng dụng CLB" required maxlength="180"></div><div class="col-md-5"><label class="form-label">Scope</label><select name="scopes[]" class="form-select" multiple required><?php foreach ($scopes as $scope): ?><option value="<?= esc($scope) ?>"><?= esc($scope) ?></option><?php endforeach; ?></select></div><div class="col-md-2"><label class="form-label">Hết hạn</label><input type="date" name="expires_at" class="form-control"></div><div class="col-md-2"><button class="btn btn-primary w-100">Tạo API key</button></div></form></div></div>
<div class="card shadow-sm"><div class="table-responsive"><table class="table table-sm align-middle mb-0"><thead><tr><th>Tên</th><th>Prefix</th><th>Scope</th><th>Trạng thái</th><th>Lần dùng cuối</th><th></th></tr></thead><tbody><?php if (!$keys): ?><tr><td colspan="6" class="text-center text-muted py-4">Chưa có API key.</td></tr><?php endif; ?><?php foreach ($keys as $key): ?><tr><td><?= esc($key->name) ?></td><td><code><?= esc($key->key_prefix) ?>…</code></td><td><?php foreach ((json_decode((string) $key->scopes, true) ?: []) as $scope): ?><span class="badge text-bg-light me-1"><?= esc($scope) ?></span><?php endforeach; ?></td><td><span class="badge text-bg-<?= $key->status === 'active' ? 'success' : 'secondary' ?>"><?= esc($key->status) ?></span></td><td><?= esc($key->last_used_at ?: 'Chưa dùng') ?></td><td><?php if ($key->status === 'active'): ?><form method="post" action="<?= base_url('admin/integrations/keys/' . $key->id . '/revoke') ?>" onsubmit="return confirm('Thu hồi API key này?')"><?= csrf_field() ?><button class="btn btn-sm btn-outline-danger">Thu hồi</button></form><?php endif; ?></td></tr><?php endforeach; ?></tbody></table></div></div>
<?= $this->endSection() ?>

<?= $this->extend('admin/layouts/main') ?>
<?= $this->section('content') ?>
<div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3"><div><h1 class="h3 mb-1">Gia hạn hội viên</h1><div class="text-muted">Ưu tiên khách sắp hết hạn để quầy chủ động chăm sóc</div></div><form class="d-flex gap-2" method="get"><select name="days" class="form-select"><option value="7" <?= $days === 7 ? 'selected' : '' ?>>7 ngày</option><option value="30" <?= $days === 30 ? 'selected' : '' ?>>30 ngày</option><option value="90" <?= $days === 90 ? 'selected' : '' ?>>90 ngày</option></select><button class="btn btn-outline-primary">Lọc</button></form></div>
<div class="card shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Khách hàng</th><th>Gói</th><th>Hết hạn</th><th>Còn lại</th><th>Giá</th><th></th></tr></thead><tbody><?php if (empty($renewals)): ?><tr><td colspan="6" class="text-center text-muted py-4">Không có hội viên cần gia hạn trong khoảng này.</td></tr><?php endif; ?><?php foreach ($renewals as $row): ?><tr><td><strong><?= esc($row->full_name) ?></strong><br><small class="text-muted"><?= esc($row->phone) ?></small></td><td><?= esc($row->package_name_vi ?? $row->package_name_en) ?></td><td><?= esc($row->end_date) ?></td><td><span class="badge <?= ((int) $row->remaining_days < 0) ? 'text-bg-danger' : 'text-bg-warning' ?>"><?= (int) $row->remaining_days ?> ngày</span></td><td><?= number_format((float) $row->price, 0, ',', '.') ?>đ</td><td class="text-end"><form method="post" action="/admin/memberships/renew/<?= (int) $row->id ?>"><button class="btn btn-sm btn-success"><i class="bi bi-arrow-repeat me-1"></i>Gia hạn</button></form></td></tr><?php endforeach; ?></tbody></table></div></div>
<?= $this->endSection() ?>

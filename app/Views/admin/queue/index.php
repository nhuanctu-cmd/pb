<?= $this->extend('layouts/admin_master') ?>
<?= $this->section('content') ?>
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h4 mb-1">Queue Monitoring</h1><p class="text-muted mb-0">Theo dõi pending, reserved, failed và dead-letter theo tenant.</p></div><span class="badge text-bg-dark">Tenant #<?= (int)($report['tenant_id'] ?? 0) ?></span></div>
<div class="row g-3 mb-4"><?php foreach (($report['summary'] ?? []) as $row): ?><div class="col-md-6 col-xl-3"><div class="card shadow-sm h-100"><div class="card-body"><div class="text-muted small"><?= esc($row->queue) ?></div><div class="fs-3 fw-semibold"><?= (int)$row->total ?></div><div class="small text-muted">Pending <?= (int)$row->pending ?> · Reserved <?= (int)$row->reserved ?> · Failed <?= (int)$row->failed ?></div></div></div></div><?php endforeach; ?><?php if (!($report['summary'] ?? [])): ?><div class="col-12"><div class="alert alert-success">Không có job queue cho tenant này.</div></div><?php endif; ?></div>
<div class="card shadow-sm"><div class="card-header">Failed jobs</div><div class="table-responsive"><table class="table table-sm align-middle mb-0"><thead><tr><th>ID</th><th>Queue</th><th>Attempts</th><th>Error</th><th>Trạng thái</th><th></th></tr></thead><tbody><?php foreach (($report['failed'] ?? []) as $job): ?><tr><td>#<?= (int)$job->id ?></td><td><?= esc($job->queue) ?></td><td><?= (int)$job->attempts ?>/<?= (int)$job->max_attempts ?></td><td><?= esc($job->error_message) ?></td><td><?= $job->dead_lettered_at ? '<span class="badge text-bg-dark">dead-letter</span>' : '<span class="badge text-bg-danger">failed</span>' ?></td><td><?php if (!$job->dead_lettered_at): ?><form method="post" action="<?= base_url('admin/queue/' . (int)$job->id . '/retry') ?>" class="d-inline"><?= csrf_field() ?><button class="btn btn-sm btn-outline-primary">Retry</button></form><form method="post" action="<?= base_url('admin/queue/' . (int)$job->id . '/dead-letter') ?>" class="d-inline ms-1"><?= csrf_field() ?><button class="btn btn-sm btn-outline-danger">Dead-letter</button></form><?php endif; ?></td></tr><?php endforeach; ?><?php if (!($report['failed'] ?? [])): ?><tr><td colspan="6" class="text-center text-muted py-4">Không có failed job.</td></tr><?php endif; ?></tbody></table></div></div>
<?= $this->endSection() ?>

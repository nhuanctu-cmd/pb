<?= $this->extend('layouts/admin_master') ?>
<?= $this->section('content') ?>
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h4 mb-1"><?= esc(lang('App.menu_webhooks')) ?></h1><p class="text-muted mb-0">Tích hợp sự kiện tenant qua HTTPS, HMAC SHA-256 và hàng đợi retry.</p></div></div>
<div class="card shadow-sm mb-4"><div class="card-body"><form method="post" action="<?= base_url('admin/webhooks/store') ?>" class="row g-2 align-items-end"><?= csrf_field() ?><div class="col-md-2"><label class="form-label">Tên</label><input name="name" class="form-control" maxlength="180" required></div><div class="col-md-3"><label class="form-label">URL HTTPS</label><input type="url" name="url" class="form-control" placeholder="https://partner.example/webhook" required></div><div class="col-md-3"><label class="form-label">Secret (tối thiểu 16)</label><input type="password" name="secret" class="form-control" minlength="16" required></div><div class="col-md-3"><label class="form-label">Events</label><select name="event_types[]" class="form-select" multiple required><?php foreach ($eventTypes as $event): ?><option value="<?= esc($event) ?>"><?= esc($event) ?></option><?php endforeach; ?></select></div><div class="col-md-1"><button class="btn btn-primary w-100">Tạo</button></div></form></div></div>
<div class="card shadow-sm"><div class="table-responsive"><table class="table table-sm align-middle mb-0"><thead><tr><th>Tên</th><th>URL</th><th>Events</th><th>Trạng thái</th><th></th></tr></thead><tbody><?php if (!$endpoints): ?><tr><td colspan="5" class="text-center text-muted py-4">Chưa có webhook endpoint.</td></tr><?php endif; ?><?php foreach ($endpoints as $endpoint): ?><tr><td><?= esc($endpoint->name) ?></td><td><?= esc($endpoint->url) ?></td><td><?php foreach ((json_decode((string) $endpoint->event_types, true) ?: []) as $event): ?><span class="badge text-bg-light me-1"><?= esc($event) ?></span><?php endforeach; ?></td><td><?= esc($endpoint->status) ?></td><td><form method="post" action="<?= base_url('admin/webhooks/'.$endpoint->id.'/status') ?>"><?= csrf_field() ?><input type="hidden" name="status" value="<?= $endpoint->status === 'active' ? 'disabled' : 'active' ?>"><button class="btn btn-sm btn-outline-secondary"><?= $endpoint->status === 'active' ? 'Tắt' : 'Bật' ?></button></form></td></tr><?php endforeach; ?></tbody></table></div></div>
<?= $this->endSection() ?>

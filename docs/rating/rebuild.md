# Rebuild

Rebuild replays official, non-disputed result versions ordered by completed time and match id. It is an explicit operation with provider, discipline, policy and dry-run options. Current profiles are only replaced after a successful non-dry-run replay.

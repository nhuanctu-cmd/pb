# Skill Levels

Skill bands are database-configured ranges. The display band is resolved from the canonical rating range; self/club/coach/external claims are stored separately and never overwrite platform calculated rating.
